# 咕宝聚合指南 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Build a resource directory website ("咕宝聚合指南") with a public homepage and a password-protected admin panel, backed by Supabase and deployed to GitHub Pages.

**Architecture:** Two static HTML pages (`index.html`, `admin.html`) sharing one CSS file and a common Supabase config. The homepage reads categories + links from Supabase and renders them as searchable tab-switched cards. The admin page uses a sidebar layout for CRUD operations on categories and links, gated by a simple password check against a Supabase-stored value.

**Tech Stack:** Vanilla HTML/CSS/JS, Supabase JS SDK (CDN), Supabase PostgreSQL, GitHub Pages

---

## File Structure

```
d:\HAHA3\juhe\
├── index.html          ← Main page: search + category tabs + link cards
├── admin.html          ← Admin page: password gate + CRUD sidebar
├── css/
│   └── style.css       ← All styles (shared by both pages)
└── js/
    ├── config.js       ← Supabase client init (shared)
    ├── main.js         ← Homepage logic (load, render, search)
    └── admin.js        ← Admin logic (auth, CRUD)
```

---

### Task 1: Create project scaffolding and Supabase config

**Files:**
- Create: `d:\HAHA3\juhe\js\config.js`
- Create: `d:\HAHA3\juhe\css\style.css` (empty placeholder)
- Create: `d:\HAHA3\juhe\js\main.js` (empty placeholder)
- Create: `d:\HAHA3\juhe\js\admin.js` (empty placeholder)

**Background:** Before writing code, the user must create a Supabase project at https://supabase.com. After creation, get the project URL and anon key from Settings → API. These will be pasted into `config.js`.

- [ ] **Step 1: Create js/config.js**

Write `d:\HAHA3\juhe\js\config.js`:

```js
// Supabase 配置 — 替换为你的项目 URL 和 anon key
const SUPABASE_URL = 'YOUR_SUPABASE_URL';
const SUPABASE_ANON_KEY = 'YOUR_SUPABASE_ANON_KEY';

const supabase = window.supabase.createClient(SUPABASE_URL, SUPABASE_ANON_KEY);
```

- [ ] **Step 2: Create empty placeholder files**

Create the following files with a one-line comment header:

`d:\HAHA3\juhe\css\style.css`:
```css
/* 咕宝聚合指南 — 样式 */
```

`d:\HAHA3\juhe\js\main.js`:
```js
// 咕宝聚合指南 — 主页逻辑
```

`d:\HAHA3\juhe\js\admin.js`:
```js
// 咕宝聚合指南 — 管理后台逻辑
```

- [ ] **Step 3: Verify files exist**

Run: `ls -R "d:\HAHA3\juhe"`

Expected: `css/style.css`, `js/config.js`, `js/main.js`, `js/admin.js` all exist.

- [ ] **Step 4: Commit**

```bash
git add js/config.js js/main.js js/admin.js css/style.css
git commit -m "chore: create project scaffolding and Supabase config"
```

---

### Task 2: Set up Supabase database tables and seed data

**Background:** This task is done in the Supabase SQL Editor. No local files are created.

- [ ] **Step 1: Open Supabase SQL Editor**

Go to your Supabase project → SQL Editor → New Query.

- [ ] **Step 2: Run table creation SQL**

Execute the following SQL:

```sql
-- 分类表
CREATE TABLE categories (
  id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  name TEXT NOT NULL,
  icon TEXT DEFAULT '📌',
  sort_order INT DEFAULT 0
);

-- 链接表
CREATE TABLE links (
  id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  category_id BIGINT REFERENCES categories(id) ON DELETE CASCADE,
  name TEXT NOT NULL,
  url TEXT NOT NULL,
  description TEXT DEFAULT '',
  sort_order INT DEFAULT 0
);

-- 启用 RLS 但允许 anon key 读取
ALTER TABLE categories ENABLE ROW LEVEL SECURITY;
ALTER TABLE links ENABLE ROW LEVEL SECURITY;

CREATE POLICY "允许公开读取分类" ON categories
  FOR SELECT USING (true);

CREATE POLICY "允许公开读取链接" ON links
  FOR SELECT USING (true);
```

- [ ] **Step 3: Insert seed data**

Execute:

```sql
-- 插入分类
INSERT INTO categories (name, icon, sort_order) VALUES
  ('影视', '🎬', 1),
  ('二次元', '🎨', 2),
  ('音乐', '🎵', 3),
  ('阅读', '📖', 4),
  ('游戏', '🎮', 5),
  ('娱乐', '🎭', 6),
  ('工具箱', '🧰', 7);

-- 插入示例链接（每个分类 6 个，使用占位数据，后续通过管理页替换）
INSERT INTO links (category_id, name, url, description, sort_order) VALUES
  (1, '示例影视站1', 'https://example.com', '影视网站描述', 1),
  (1, '示例影视站2', 'https://example.com', '影视网站描述', 2),
  (1, '示例影视站3', 'https://example.com', '影视网站描述', 3),
  (1, '示例影视站4', 'https://example.com', '影视网站描述', 4),
  (1, '示例影视站5', 'https://example.com', '影视网站描述', 5),
  (1, '示例影视站6', 'https://example.com', '影视网站描述', 6),
  (2, '示例二次元站1', 'https://example.com', '二次元网站描述', 1),
  (2, '示例二次元站2', 'https://example.com', '二次元网站描述', 2),
  (2, '示例二次元站3', 'https://example.com', '二次元网站描述', 3),
  (2, '示例二次元站4', 'https://example.com', '二次元网站描述', 4),
  (2, '示例二次元站5', 'https://example.com', '二次元网站描述', 5),
  (2, '示例二次元站6', 'https://example.com', '二次元网站描述', 6),
  (3, '示例音乐站1', 'https://example.com', '音乐网站描述', 1),
  (3, '示例音乐站2', 'https://example.com', '音乐网站描述', 2),
  (3, '示例音乐站3', 'https://example.com', '音乐网站描述', 3),
  (3, '示例音乐站4', 'https://example.com', '音乐网站描述', 4),
  (3, '示例音乐站5', 'https://example.com', '音乐网站描述', 5),
  (3, '示例音乐站6', 'https://example.com', '音乐网站描述', 6),
  (4, '示例阅读站1', 'https://example.com', '阅读网站描述', 1),
  (4, '示例阅读站2', 'https://example.com', '阅读网站描述', 2),
  (4, '示例阅读站3', 'https://example.com', '阅读网站描述', 3),
  (4, '示例阅读站4', 'https://example.com', '阅读网站描述', 4),
  (4, '示例阅读站5', 'https://example.com', '阅读网站描述', 5),
  (4, '示例阅读站6', 'https://example.com', '阅读网站描述', 6),
  (5, '示例游戏站1', 'https://example.com', '游戏网站描述', 1),
  (5, '示例游戏站2', 'https://example.com', '游戏网站描述', 2),
  (5, '示例游戏站3', 'https://example.com', '游戏网站描述', 3),
  (5, '示例游戏站4', 'https://example.com', '游戏网站描述', 4),
  (5, '示例游戏站5', 'https://example.com', '游戏网站描述', 5),
  (5, '示例游戏站6', 'https://example.com', '游戏网站描述', 6),
  (6, '示例娱乐站1', 'https://example.com', '娱乐网站描述', 1),
  (6, '示例娱乐站2', 'https://example.com', '娱乐网站描述', 2),
  (6, '示例娱乐站3', 'https://example.com', '娱乐网站描述', 3),
  (6, '示例娱乐站4', 'https://example.com', '娱乐网站描述', 4),
  (6, '示例娱乐站5', 'https://example.com', '娱乐网站描述', 5),
  (6, '示例娱乐站6', 'https://example.com', '娱乐网站描述', 6),
  (7, '示例工具站1', 'https://example.com', '工具网站描述', 1),
  (7, '示例工具站2', 'https://example.com', '工具网站描述', 2),
  (7, '示例工具站3', 'https://example.com', '工具网站描述', 3),
  (7, '示例工具站4', 'https://example.com', '工具网站描述', 4),
  (7, '示例工具站5', 'https://example.com', '工具网站描述', 5),
  (7, '示例工具站6', 'https://example.com', '工具网站描述', 6);
```

- [ ] **Step 4: Create admin password config table**

Execute:

```sql
CREATE TABLE config (
  key TEXT PRIMARY KEY,
  value TEXT NOT NULL
);

INSERT INTO config (key, value) VALUES ('admin_password', 'gubao2024');

CREATE POLICY "允许公开读取配置" ON config
  FOR SELECT USING (true);
```

- [ ] **Step 5: Verify data**

Run in Supabase Table Editor: check that `categories` has 7 rows, `links` has 42 rows, `config` has 1 row.

- [ ] **Step 6: Commit**

```bash
git commit --allow-empty -m "chore: Supabase database setup and seed data"
```

---

### Task 3: Write CSS styles

**Files:**
- Write: `d:\HAHA3\juhe\css\style.css`

- [ ] **Step 1: Write the complete stylesheet**

Write `d:\HAHA3\juhe\css\style.css`:

```css
/* 咕宝聚合指南 — 样式 */
*, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }

body {
  font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', 'PingFang SC', 'Microsoft YaHei', sans-serif;
  background: linear-gradient(135deg, #f0f4ff 0%, #f8f6ff 50%, #eef3fa 100%);
  min-height: 100vh;
  color: #3a3d66;
}

/* ====== 主页 ====== */
.container { max-width: 900px; margin: 0 auto; padding: 32px 20px 60px; }

/* Header */
.header { display: flex; justify-content: space-between; align-items: center; margin-bottom: 24px; }
.logo { font-size: 22px; font-weight: 700; color: #3a3d66; }

/* 搜索栏 */
.search-wrapper { display: flex; justify-content: center; margin-bottom: 28px; }
.search-input {
  width: 100%; max-width: 480px; padding: 12px 20px;
  border: none; border-radius: 24px; background: #fff;
  box-shadow: 0 2px 12px rgba(80,80,200,0.08);
  font-size: 15px; color: #3a3d66; outline: none;
}
.search-input::placeholder { color: #aabbcc; }

/* 分类 Tab */
.tabs { display: flex; gap: 8px; flex-wrap: wrap; justify-content: center; margin-bottom: 24px; }
.tab {
  padding: 8px 18px; border-radius: 20px; border: none; cursor: pointer;
  font-size: 14px; background: rgba(0,0,0,0.04); color: #8899bb;
  transition: all 0.2s;
}
.tab:hover { background: rgba(90,120,220,0.1); color: #5a78dc; }
.tab.active { background: #5a78dc; color: #fff; }

/* 链接列表 */
.links-section { }
.section-title { font-size: 14px; color: #8899bb; margin-bottom: 12px; }

/* 链接卡片 */
.link-list { display: flex; flex-direction: column; gap: 8px; }
.link-card {
  display: flex; align-items: center; gap: 14px; padding: 14px 18px;
  background: #fff; border-radius: 12px; cursor: pointer;
  box-shadow: 0 1px 8px rgba(0,0,0,0.04);
  border-left: 3px solid transparent;
  transition: all 0.15s;
  text-decoration: none; color: inherit;
}
.link-card:hover { border-left-color: #5a78dc; box-shadow: 0 2px 16px rgba(80,80,200,0.1); transform: translateX(2px); }
.link-icon { font-size: 24px; flex-shrink: 0; }
.link-info { flex: 1; min-width: 0; }
.link-name { font-size: 15px; font-weight: 600; color: #3a3d66; }
.link-desc { font-size: 12px; color: #8899bb; margin-top: 2px; white-space: nowrap; overflow: hidden; text-overflow: ellipsis; }
.link-arrow { color: #c0c8dd; font-size: 14px; flex-shrink: 0; }

/* 搜索模式 */
.search-mode .tabs { display: none; }
.search-mode .section-title { display: none; }

/* 空状态 */
.empty-state { text-align: center; padding: 40px 20px; color: #8899bb; font-size: 14px; }

/* ====== 管理后台 ====== */
.admin-body { font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', 'PingFang SC', 'Microsoft YaHei', sans-serif; }

/* 密码门 */
.password-gate { display: flex; align-items: center; justify-content: center; min-height: 100vh; }
.password-box {
  background: #fff; border-radius: 16px; padding: 40px; text-align: center;
  box-shadow: 0 4px 24px rgba(0,0,0,0.06); width: 360px;
}
.password-box h2 { font-size: 20px; color: #3a3d66; margin-bottom: 20px; }
.password-box input {
  width: 100%; padding: 10px 16px; border: 1px solid #e0e4f0; border-radius: 10px;
  font-size: 15px; outline: none; margin-bottom: 12px;
}
.password-box input:focus { border-color: #5a78dc; }
.password-box button {
  width: 100%; padding: 10px; background: #5a78dc; color: #fff;
  border: none; border-radius: 10px; font-size: 15px; cursor: pointer;
}
.password-error { color: #e05555; font-size: 13px; margin-bottom: 8px; display: none; }

/* 管理主界面 */
.admin-panel { display: none; height: 100vh; }
.admin-header { background: #fff; padding: 12px 20px; border-bottom: 1px solid #f0f0f5; display: flex; justify-content: space-between; align-items: center; }
.admin-header .logo { font-size: 16px; }
.btn-logout { background: none; border: 1px solid #e0e4f0; padding: 6px 14px; border-radius: 8px; cursor: pointer; font-size: 13px; color: #8899bb; }

.admin-body-layout { display: flex; height: calc(100vh - 53px); }

/* 侧边栏 */
.sidebar {
  width: 220px; background: #fff; border-right: 1px solid #f0f0f5;
  padding: 16px 0; display: flex; flex-direction: column; overflow-y: auto;
}
.sidebar-title { font-size: 12px; color: #aabbcc; padding: 0 16px; margin-bottom: 8px; text-transform: uppercase; letter-spacing: 1px; }
.cat-item {
  display: flex; align-items: center; gap: 8px; padding: 10px 16px; cursor: pointer;
  font-size: 14px; color: #3a3d66; transition: background 0.15s;
}
.cat-item:hover { background: #f8f9ff; }
.cat-item.active { background: rgba(90,120,220,0.08); color: #5a78dc; font-weight: 600; }
.cat-item .cat-count { font-size: 11px; color: #aabbcc; margin-left: auto; }
.cat-item .cat-actions { display: none; gap: 4px; margin-left: 4px; }
.cat-item:hover .cat-actions { display: flex; }
.cat-item:hover .cat-count { display: none; }
.btn-icon {
  background: none; border: none; cursor: pointer; font-size: 13px;
  padding: 2px 4px; border-radius: 4px; color: #8899bb;
}
.btn-icon:hover { background: #f0f0f5; color: #3a3d66; }
.btn-icon.danger:hover { background: #fff0f0; color: #e05555; }

.sidebar-footer { padding: 12px 16px; border-top: 1px solid #f0f0f5; }
.btn-add-cat {
  width: 100%; padding: 8px; background: #f0f4ff; color: #5a78dc;
  border: 1px dashed #5a78dc; border-radius: 8px; font-size: 13px; cursor: pointer;
}

/* 右侧内容 */
.main-area { flex: 1; padding: 24px 32px; overflow-y: auto; background: linear-gradient(135deg, #f0f4ff 0%, #f8f6ff 50%, #eef3fa 100%); }
.main-header { display: flex; justify-content: space-between; align-items: center; margin-bottom: 20px; }
.main-header h3 { font-size: 18px; color: #3a3d66; }
.main-header span { font-size: 13px; color: #8899bb; }

.link-row {
  display: flex; align-items: center; gap: 12px; padding: 12px 16px;
  background: #fff; border-radius: 10px; margin-bottom: 6px;
  box-shadow: 0 1px 4px rgba(0,0,0,0.03);
}
.link-row .lr-name { font-weight: 600; font-size: 14px; color: #3a3d66; flex: 1; }
.link-row .lr-url { font-size: 12px; color: #8899bb; flex: 2; white-space: nowrap; overflow: hidden; text-overflow: ellipsis; }
.link-row .lr-actions { display: flex; gap: 4px; flex-shrink: 0; }

.btn-add-link {
  padding: 10px 20px; background: #5a78dc; color: #fff;
  border: none; border-radius: 8px; font-size: 14px; cursor: pointer; margin-top: 8px;
}

/* Modal */
.modal-overlay {
  display: none; position: fixed; inset: 0; background: rgba(0,0,0,0.3);
  justify-content: center; align-items: center; z-index: 100;
}
.modal-overlay.show { display: flex; }
.modal {
  background: #fff; border-radius: 16px; padding: 28px; width: 420px;
  box-shadow: 0 8px 40px rgba(0,0,0,0.12);
}
.modal h4 { font-size: 16px; color: #3a3d66; margin-bottom: 18px; }
.modal label { display: block; font-size: 13px; color: #8899bb; margin-bottom: 4px; margin-top: 12px; }
.modal label:first-of-type { margin-top: 0; }
.modal input, .modal textarea {
  width: 100%; padding: 9px 12px; border: 1px solid #e0e4f0; border-radius: 8px;
  font-size: 14px; outline: none; font-family: inherit;
}
.modal input:focus, .modal textarea:focus { border-color: #5a78dc; }
.modal textarea { resize: vertical; min-height: 60px; }
.modal-actions { display: flex; gap: 8px; justify-content: flex-end; margin-top: 20px; }
.btn-cancel { padding: 8px 18px; background: #f0f0f5; border: none; border-radius: 8px; cursor: pointer; font-size: 14px; color: #8899bb; }
.btn-save { padding: 8px 18px; background: #5a78dc; color: #fff; border: none; border-radius: 8px; cursor: pointer; font-size: 14px; }
.btn-delete-confirm { padding: 8px 18px; background: #e05555; color: #fff; border: none; border-radius: 8px; cursor: pointer; font-size: 14px; }

/* 提示消息 */
.toast {
  position: fixed; bottom: 24px; left: 50%; transform: translateX(-50%);
  background: #3a3d66; color: #fff; padding: 10px 24px; border-radius: 20px;
  font-size: 14px; z-index: 200; opacity: 0; transition: opacity 0.3s;
}
.toast.show { opacity: 1; }

/* 响应式 */
@media (max-width: 640px) {
  .container { padding: 20px 12px 40px; }
  .logo { font-size: 18px; }
  .search-input { max-width: 100%; }
  .tabs { gap: 6px; }
  .tab { padding: 6px 14px; font-size: 13px; }
  .link-card { padding: 12px 14px; }
  .admin-body-layout { flex-direction: column; }
  .sidebar { width: 100%; flex-direction: row; overflow-x: auto; padding: 8px; border-right: none; border-bottom: 1px solid #f0f0f5; flex-shrink: 0; }
  .sidebar-title { display: none; }
  .sidebar-footer { display: none; }
  .cat-item { white-space: nowrap; padding: 6px 12px; font-size: 13px; }
  .main-area { padding: 16px; }
  .modal { width: 90%; margin: 0 16px; }
}
```

- [ ] **Step 2: Verify the file**

Run: `wc -l "d:\HAHA3\juhe\css\style.css"` — expect ~200 lines.

- [ ] **Step 3: Commit**

```bash
git add css/style.css
git commit -m "feat: add complete stylesheet for homepage and admin panel"
```

---

### Task 4: Build main page HTML + JS (data loading and rendering)

**Files:**
- Create: `d:\HAHA3\juhe\index.html`
- Modify: `d:\HAHA3\juhe\js\main.js`

- [ ] **Step 1: Write index.html**

Write `d:\HAHA3\juhe\index.html`:

```html
<!DOCTYPE html>
<html lang="zh-CN">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>咕宝聚合指南</title>
  <link rel="stylesheet" href="css/style.css">
  <script src="https://cdn.jsdelivr.net/npm/@supabase/supabase-js@2"></script>
</head>
<body>
  <div class="container">
    <header class="header">
      <div class="logo">✨ 咕宝聚合指南</div>
    </header>

    <div class="search-wrapper">
      <input type="text" class="search-input" id="searchInput" placeholder="搜索网站名称或关键词...">
    </div>

    <div class="tabs" id="tabs"></div>

    <div class="links-section" id="linksSection">
      <div class="section-title" id="sectionTitle"></div>
      <div class="link-list" id="linkList"></div>
    </div>
  </div>

  <script src="js/config.js"></script>
  <script src="js/main.js"></script>
</body>
</html>
```

- [ ] **Step 2: Write main.js — data loading and rendering**

Write `d:\HAHA3\juhe\js\main.js`:

```js
// 咕宝聚合指南 — 主页逻辑
let categories = [];
let links = [];
let activeCategoryId = null;

// 加载数据
async function loadData() {
  const [catRes, linkRes] = await Promise.all([
    supabase.from('categories').select('*').order('sort_order'),
    supabase.from('links').select('*').order('sort_order')
  ]);

  if (catRes.error) { console.error('加载分类失败:', catRes.error); return; }
  if (linkRes.error) { console.error('加载链接失败:', linkRes.error); return; }

  categories = catRes.data;
  links = linkRes.data;

  if (categories.length > 0) {
    activeCategoryId = categories[0].id;
    renderTabs();
    renderLinks();
  }
}

// 渲染分类 Tab
function renderTabs() {
  const tabsEl = document.getElementById('tabs');
  tabsEl.innerHTML = categories.map(cat =>
    `<button class="tab${cat.id === activeCategoryId ? ' active' : ''}" data-id="${cat.id}">
      ${cat.icon} ${cat.name}
    </button>`
  ).join('');

  tabsEl.querySelectorAll('.tab').forEach(btn => {
    btn.addEventListener('click', () => {
      activeCategoryId = parseInt(btn.dataset.id);
      renderTabs();
      renderLinks();
    });
  });
}

// 渲染链接列表
function renderLinks(filterText = '') {
  const activeCat = categories.find(c => c.id === activeCategoryId);
  const titleEl = document.getElementById('sectionTitle');
  const listEl = document.getElementById('linkList');

  let filteredLinks = links.filter(l => l.category_id === activeCategoryId);
  if (filterText) {
    filteredLinks = links.filter(l =>
      l.name.toLowerCase().includes(filterText) ||
      (l.description && l.description.toLowerCase().includes(filterText))
    );
  }

  if (filterText) {
    titleEl.textContent = `搜索结果 · ${filteredLinks.length} 个`;
  } else {
    titleEl.textContent = activeCat ? `${activeCat.icon} ${activeCat.name} · ${filteredLinks.length} 个网站` : '';
  }

  if (filteredLinks.length === 0) {
    listEl.innerHTML = '<div class="empty-state">没有找到匹配的网站 🤔</div>';
    return;
  }

  listEl.innerHTML = filteredLinks.map(link => `
    <a href="${escapeHtml(link.url)}" target="_blank" rel="noopener" class="link-card">
      <span class="link-icon">${activeCat ? activeCat.icon : '🔗'}</span>
      <div class="link-info">
        <div class="link-name">${escapeHtml(link.name)}</div>
        ${link.description ? `<div class="link-desc">${escapeHtml(link.description)}</div>` : ''}
      </div>
      <span class="link-arrow">→</span>
    </a>
  `).join('');
}

function escapeHtml(str) {
  const div = document.createElement('div');
  div.textContent = str;
  return div.innerHTML;
}

// 搜索处理
function initSearch() {
  const input = document.getElementById('searchInput');
  const container = document.querySelector('.container');

  input.addEventListener('input', () => {
    const val = input.value.trim().toLowerCase();
    if (val) {
      container.classList.add('search-mode');
      activeCategoryId = null;
      renderTabs();
      renderLinks(val);
    } else {
      container.classList.remove('search-mode');
      activeCategoryId = categories.length > 0 ? categories[0].id : null;
      renderTabs();
      renderLinks();
    }
  });
}

// 启动
loadData().then(() => initSearch());
```

- [ ] **Step 3: Open index.html in browser**

Run a local server or open the file directly. Verify:
- Page loads with "✨ 咕宝聚合指南" header
- Search bar is visible
- 7 category tabs appear
- First category's links render as cards
- Clicking tabs switches the link list

- [ ] **Step 4: Commit**

```bash
git add index.html js/main.js
git commit -m "feat: add homepage with data loading and tab switching"
```

---

### Task 5: Build admin page HTML + password gate

**Files:**
- Create: `d:\HAHA3\juhe\admin.html`
- Modify: `d:\HAHA3\juhe\js\admin.js`

- [ ] **Step 1: Write admin.html**

Write `d:\HAHA3\juhe\admin.html`:

```html
<!DOCTYPE html>
<html lang="zh-CN">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>管理后台 · 咕宝聚合指南</title>
  <link rel="stylesheet" href="css/style.css">
  <script src="https://cdn.jsdelivr.net/npm/@supabase/supabase-js@2"></script>
</head>
<body class="admin-body" style="background: linear-gradient(135deg, #f0f4ff 0%, #f8f6ff 50%, #eef3fa 100%); min-height: 100vh;">

  <!-- 密码门 -->
  <div class="password-gate" id="passwordGate">
    <div class="password-box">
      <h2>🔐 管理后台</h2>
      <div class="password-error" id="passwordError">密码错误，请重试</div>
      <input type="password" id="passwordInput" placeholder="请输入管理密码" autocomplete="off">
      <button id="loginBtn">进入管理</button>
    </div>
  </div>

  <!-- 管理主界面 -->
  <div class="admin-panel" id="adminPanel">
    <div class="admin-header">
      <div class="logo">⚙️ 管理后台</div>
      <button class="btn-logout" id="logoutBtn">退出</button>
    </div>
    <div class="admin-body-layout">
      <aside class="sidebar" id="sidebar">
        <div class="sidebar-title">分类</div>
        <div id="catList"></div>
        <div class="sidebar-footer">
          <button class="btn-add-cat" id="btnAddCat">+ 添加分类</button>
        </div>
      </aside>
      <main class="main-area" id="mainArea">
        <div class="main-header">
          <div>
            <h3 id="mainTitle">选择一个分类</h3>
            <span id="mainCount"></span>
          </div>
          <button class="btn-add-link" id="btnAddLink" style="display:none;">+ 添加链接</button>
        </div>
        <div id="linkRows"></div>
      </main>
    </div>
  </div>

  <!-- Modal -->
  <div class="modal-overlay" id="modalOverlay">
    <div class="modal" id="modalContent"></div>
  </div>

  <!-- Toast -->
  <div class="toast" id="toast"></div>

  <script src="js/config.js"></script>
  <script src="js/admin.js"></script>
</body>
</html>
```

- [ ] **Step 2: Write admin.js — password gate and data loading**

Write `d:\HAHA3\juhe\js\admin.js`:

```js
// 咕宝聚合指南 — 管理后台逻辑
let adminCategories = [];
let adminLinks = [];
let selectedCategoryId = null;
let adminPassword = '';

// ====== 工具函数 ======
function $(id) { return document.getElementById(id); }
function escapeHtml(str) { const div = document.createElement('div'); div.textContent = str; return div.innerHTML; }

function showToast(msg) {
  const t = $('toast');
  t.textContent = msg;
  t.classList.add('show');
  setTimeout(() => t.classList.remove('show'), 2000);
}

// ====== 密码门 ======
async function loadAdminPassword() {
  const { data, error } = await supabase.from('config').select('value').eq('key', 'admin_password').single();
  if (error) { console.error('加载密码配置失败:', error); return; }
  adminPassword = data.value;
}

async function initLogin() {
  await loadAdminPassword();
  $('loginBtn').addEventListener('click', () => {
    const input = $('passwordInput').value;
    if (input === adminPassword) {
      $('passwordGate').style.display = 'none';
      $('adminPanel').style.display = 'flex';
      $('adminPanel').style.flexDirection = 'column';
      loadAdminData();
    } else {
      $('passwordError').style.display = 'block';
    }
  });
  $('passwordInput').addEventListener('keydown', (e) => {
    if (e.key === 'Enter') $('loginBtn').click();
  });
  $('logoutBtn').addEventListener('click', () => {
    $('adminPanel').style.display = 'none';
    $('passwordGate').style.display = 'flex';
    $('passwordInput').value = '';
    $('passwordError').style.display = 'none';
  });
}

// ====== 数据加载 ======
async function loadAdminData() {
  const [catRes, linkRes] = await Promise.all([
    supabase.from('categories').select('*').order('sort_order'),
    supabase.from('links').select('*').order('sort_order')
  ]);
  if (catRes.error) { console.error(catRes.error); return; }
  if (linkRes.error) { console.error(linkRes.error); return; }
  adminCategories = catRes.data;
  adminLinks = linkRes.data;
  renderSidebar();
  if (adminCategories.length > 0) {
    selectCategory(adminCategories[0].id);
  }
}

// ====== 侧边栏渲染 ======
function renderSidebar() {
  const list = $('catList');
  list.innerHTML = adminCategories.map(cat => {
    const count = adminLinks.filter(l => l.category_id === cat.id).length;
    return `
      <div class="cat-item${cat.id === selectedCategoryId ? ' active' : ''}" data-id="${cat.id}" onclick="selectCategory(${cat.id})">
        <span>${cat.icon}</span>
        <span>${escapeHtml(cat.name)}</span>
        <span class="cat-count">${count}</span>
        <span class="cat-actions">
          <button class="btn-icon" onclick="event.stopPropagation();editCategory(${cat.id})" title="编辑">✏️</button>
          <button class="btn-icon danger" onclick="event.stopPropagation();deleteCategory(${cat.id})" title="删除">🗑️</button>
        </span>
      </div>`;
  }).join('');
}

function selectCategory(id) {
  selectedCategoryId = id;
  renderSidebar();
  renderLinks();
}

// ====== 链接列表渲染 ======
function renderLinks() {
  const cat = adminCategories.find(c => c.id === selectedCategoryId);
  if (!cat) { $('linkRows').innerHTML = ''; return; }

  const catLinks = adminLinks.filter(l => l.category_id === selectedCategoryId);
  $('mainTitle').textContent = `${cat.icon} ${cat.name}`;
  $('mainCount').textContent = `${catLinks.length} 个链接`;
  $('btnAddLink').style.display = 'block';

  $('linkRows').innerHTML = catLinks.map(link => `
    <div class="link-row">
      <span class="lr-name">${escapeHtml(link.name)}</span>
      <span class="lr-url">${escapeHtml(link.url)}</span>
      <span class="lr-actions">
        <button class="btn-icon" onclick="editLink(${link.id})" title="编辑">✏️</button>
        <button class="btn-icon danger" onclick="deleteLink(${link.id})" title="删除">🗑️</button>
      </span>
    </div>
  `).join('') || '<div class="empty-state">暂无链接，点击下方按钮添加</div>';
}
```

- [ ] **Step 3: Verify login flow**

Open `admin.html` in a browser:
- Password gate appears
- Enter wrong password → error message shows
- Enter correct password ("gubao2024") → admin panel appears
- Sidebar shows 7 categories with link counts
- Clicking a category shows its links in the main area

- [ ] **Step 4: Commit**

```bash
git add admin.html js/admin.js
git commit -m "feat: add admin page with password gate and data display"
```

---

### Task 6: Build admin CRUD operations (categories + links)

**Files:**
- Modify: `d:\HAHA3\juhe\js\admin.js` (append CRUD functions + modal logic)

- [ ] **Step 1: Append CRUD functions to admin.js**

Append the following to `d:\HAHA3\juhe\js\admin.js`:

```js
// ====== Modal ======
function openModal(title, fields, onSave) {
  const overlay = $('modalOverlay');
  $('modalContent').innerHTML = `
    <h4>${title}</h4>
    ${fields.map(f => f.type === 'delete'
      ? `<p style="color:#8899bb;font-size:14px;margin-top:8px;">${f.label}</p>`
      : `<label>${f.label}</label>
        ${f.type === 'textarea'
          ? `<textarea id="field_${f.key}" placeholder="${f.placeholder || ''}">${escapeHtml(f.value || '')}</textarea>`
          : `<input type="text" id="field_${f.key}" placeholder="${f.placeholder || ''}" value="${escapeHtml(f.value || '')}">`}`
    ).join('')}
    <div class="modal-actions">
      <button class="btn-cancel" onclick="closeModal()">取消</button>
      ${f.type === 'delete'
        ? `<button class="btn-delete-confirm" id="btnSave">确认删除</button>`
        : `<button class="btn-save" id="btnSave">保存</button>`}
    </div>
  `;
  overlay.classList.add('show');
  $('btnSave').addEventListener('click', async () => {
    const values = {};
    fields.filter(f => f.type !== 'delete').forEach(f => {
      values[f.key] = document.getElementById(`field_${f.key}`).value;
    });
    await onSave(values);
    closeModal();
  });
}

function closeModal() {
  $('modalOverlay').classList.remove('show');
}

// ====== 分类 CRUD ======
function editCategory(id) {
  const cat = adminCategories.find(c => c.id === id);
  if (!cat) return;
  openModal('编辑分类', [
    { key: 'name', label: '名称', value: cat.name },
    { key: 'icon', label: '图标 (emoji)', value: cat.icon, placeholder: '例如 🎬' }
  ], async (values) => {
    await supabase.from('categories').update({ name: values.name, icon: values.icon }).eq('id', id);
    showToast('分类已更新');
    loadAdminData();
  });
}

async function deleteCategory(id) {
  const cat = adminCategories.find(c => c.id === id);
  if (!cat) return;
  openModal(`删除分类: ${cat.icon} ${cat.name}`, [
    { key: 'confirm', label: '删除分类将同时删除该分类下的所有链接，确定要删除吗？', type: 'delete' }
  ], async () => {
    await supabase.from('categories').delete().eq('id', id);
    showToast('分类已删除');
    selectedCategoryId = null;
    loadAdminData();
  });
}

async function addCategory() {
  openModal('添加分类', [
    { key: 'name', label: '名称', placeholder: '例如 学习' },
    { key: 'icon', label: '图标 (emoji)', placeholder: '例如 📚' }
  ], async (values) => {
    const maxSort = adminCategories.reduce((max, c) => Math.max(max, c.sort_order), 0);
    await supabase.from('categories').insert({
      name: values.name, icon: values.icon || '📌', sort_order: maxSort + 1
    });
    showToast('分类已添加');
    loadAdminData();
  });
}

// ====== 链接 CRUD ======
function editLink(id) {
  const link = adminLinks.find(l => l.id === id);
  if (!link) return;
  openModal('编辑链接', [
    { key: 'name', label: '网站名称', value: link.name },
    { key: 'url', label: '网址', value: link.url, placeholder: 'https://...' },
    { key: 'description', label: '描述', value: link.description || '', type: 'textarea', placeholder: '简短描述' }
  ], async (values) => {
    await supabase.from('links').update({
      name: values.name, url: values.url, description: values.description
    }).eq('id', id);
    showToast('链接已更新');
    loadAdminData();
  });
}

async function deleteLink(id) {
  const link = adminLinks.find(l => l.id === id);
  if (!link) return;
  openModal(`删除链接: ${link.name}`, [
    { key: 'confirm', label: '确定要删除这个链接吗？', type: 'delete' }
  ], async () => {
    await supabase.from('links').delete().eq('id', id);
    showToast('链接已删除');
    loadAdminData();
  });
}

async function addLink() {
  if (!selectedCategoryId) return;
  openModal('添加链接', [
    { key: 'name', label: '网站名称', placeholder: '例如 低端影视' },
    { key: 'url', label: '网址', placeholder: 'https://...' },
    { key: 'description', label: '描述', type: 'textarea', placeholder: '简短描述（可选）' }
  ], async (values) => {
    const catLinks = adminLinks.filter(l => l.category_id === selectedCategoryId);
    const maxSort = catLinks.reduce((max, l) => Math.max(max, l.sort_order), 0);
    await supabase.from('links').insert({
      category_id: selectedCategoryId,
      name: values.name, url: values.url, description: values.description || '',
      sort_order: maxSort + 1
    });
    showToast('链接已添加');
    loadAdminData();
  });
}

// ====== 事件绑定 ======
$('btnAddCat').addEventListener('click', addCategory);
$('btnAddLink').addEventListener('click', addLink);

// 关闭 modal（点击遮罩）
$('modalOverlay').addEventListener('click', (e) => {
  if (e.target === $('modalOverlay')) closeModal();
});

// ====== 启动 ======
initLogin();
```

- [ ] **Step 2: Verify CRUD operations**

Open `admin.html` (after login):
- Click "+ 添加分类" → modal opens → fill in name "测试" and icon "🧪" → save → new category appears in sidebar
- Click edit (✏️) on the new category → change name → save → name updates
- Click a category → click "+ 添加链接" → fill in name/url/description → save → link appears
- Click edit on a link → change values → save → link updates
- Click delete on a link → confirm → link removed
- Click delete on "测试" category → confirm → category and its links removed

- [ ] **Step 3: Commit**

```bash
git add js/admin.js
git commit -m "feat: add CRUD operations for categories and links in admin panel"
```

---

### Task 7: Final integration verification

- [ ] **Step 1: Verify homepage with real data**

Open `index.html` in browser:
- All 7 categories load as tabs
- Each tab shows 6 example links
- Clicking tabs switches link list correctly
- Search bar filters links across categories
- Clicking a link card opens the URL in a new tab
- Mobile viewport: layout adapts (tabs wrap, cards full width)

- [ ] **Step 2: Verify admin full flow**

Open `admin.html`:
- Login with "gubao2024" → admin panel shows
- Add a real website link to each category (replace example data)
- Edit a link URL and save
- Delete a link
- Login error on wrong password works
- Logout and re-login works

- [ ] **Step 3: Add .gitignore**

Write `d:\HAHA3\juhe\.gitignore`:

```
.superpowers/
```

- [ ] **Step 4: Commit**

```bash
git add .gitignore
git commit -m "chore: add .gitignore and final integration verification"
```
